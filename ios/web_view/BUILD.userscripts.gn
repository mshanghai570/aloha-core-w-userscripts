# BUILD.userscripts.gn - minimal userscript build entries (stub)
# Add real sources and dependencies as needed.

# Define a cwv_source_set for userscript_manager so other targets can depend on it.
# Populate `sources` and `deps` with the real implementation files when available.
cwv_source_set("userscript_manager") {
  sources = [
    # Add UserscriptManager implementation source(s) here, e.g.: "userscript_manager.mm",
  ]
  deps = [
    # Add necessary dependencies here.
  ]
}
